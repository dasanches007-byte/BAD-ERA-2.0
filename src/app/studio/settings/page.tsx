import { RouteShell } from "@/components/ui/route-shell";

export const metadata = { title: "Settings" };

export default function Page() {
  return <RouteShell area="Studio" title="Settings" phase="Phase 3" />;
}
