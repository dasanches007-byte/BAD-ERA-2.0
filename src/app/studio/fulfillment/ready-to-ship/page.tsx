import { RouteShell } from "@/components/ui/route-shell";

export const metadata = { title: "Ready to Ship" };

export default function Page() {
  return <RouteShell area="Studio" title="Ready to Ship" phase="Phase 6" />;
}
