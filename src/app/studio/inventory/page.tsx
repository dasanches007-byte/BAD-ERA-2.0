import { RouteShell } from "@/components/ui/route-shell";

export const metadata = { title: "Inventory" };

export default function Page() {
  return <RouteShell area="Studio" title="Inventory" phase="Phase 3" />;
}
